启动容器
cd E:\GongZuoTai\VocaLogue
docker-compose up -d

验证数据库连接
docker exec -it lingoai-postgres psql -U admin -d lingoai


mvn spring-boot:run


mvn spring-boot:run 2>&1


手动分步启动（不依赖 start.bat）
# 1. 数据库
docker compose up -d postgres redis

# 2. ASR 服务
cd asr-service
pip install -r requirements.txt
python generate_proto.py
python -m app.server --device cpu --model large-v2 --compute-type int8

# 3. Backend（新终端）
cd backend
mvn spring-boot:run

# 4. Frontend（新终端）
cd frontend
npm run dev



# 手动清理 + 启动 ASR
cd asr-service
Remove-Item -Recurse -Force app\__pycache__ -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force app\proto\__pycache__ -ErrorAction SilentlyContinue
python -m app.server --device cpu --model medium --compute-type int8